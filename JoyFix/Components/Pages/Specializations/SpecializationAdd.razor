@page "/specializationadd"
@inject JoyFix.Services.SpecializationService SpecializationService
@inject NavigationManager Navigation
@rendermode InteractiveServer

<h3>Dodaj nową specjalizację</h3>

<EditForm Model="specialization" OnValidSubmit="HandleValidSubmit">
    <DataAnnotationsValidator />
    <ValidationSummary />

    <div class="mb-3">
        <label class="form-label">Nazwa</label>
        <InputText class="form-control" @bind-Value="specialization.Name" />
    </div>
    <div class="mb-3">
        <label class="form-label">Opis</label>
        <InputTextArea class="form-control" @bind-Value="specialization.Description" />
    </div>

    <button class="btn btn-success" type="submit">Zapisz</button>
    <button class="btn btn-secondary ms-2" type="button" @onclick="GoBack">Anuluj</button>
</EditForm>

@code {
    private JoyFix.Data.Specialization specialization = new();

    private void HandleValidSubmit()
    {
        SpecializationService.AddSpecialization(specialization);
        Navigation.NavigateTo("/specializations");
    }

    private void GoBack()
    {
        Navigation.NavigateTo("/specializations");
    }
}
